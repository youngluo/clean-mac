## 1. 修复快照识别

- [x] 1.1 过滤不同 macOS 版本的 `Snapshots for ...:` 标题格式。
- [x] 1.2 只识别 `com.apple.TimeMachine.*` 快照，排除系统更新快照和非结构化输出。
- [x] 1.3 无可维护快照时不创建空候选，保留扫描诊断。

## 2. 测试和验证

- [x] 2.1 补充新版标题、系统更新快照和实际 Time Machine 快照的解析测试。
- [x] 2.2 执行 Debug 构建，关闭旧实例并启动最新 CleanMac。

验证说明：Debug 构建、测试构建通过；定向 XCTest 受既有 runner 启动问题阻塞，未进入断言执行。
