## 1. 扫描提示区域

- [x] 1.1 将空闲态辅助文案与扫描态提示提取为同一个视觉元素，只更新扫描描述文案；验证两种状态的字号、颜色、内边距和圆球间距一致
- [x] 1.2 保持共享提示文本单行，超长文案末尾省略且不显示分类名称；验证扫描中提示随当前描述更新，provider 尚未建立时显示准备扫描文案
- [x] 1.3 调整扫描态提示与 provider 面板的间距，使其等于空闲态提示与授权提示面板的间距；验证其他面板间距不受影响

## 2. Provider 面板收敛

- [x] 2.1 移除扫描中 provider 面板的“扫描进度”标题和动态说明，移除空状态下的准备扫描提示；验证扫描中面板仅显示 provider 状态行
- [x] 2.2 调整等待确认、清理中和结果状态的 provider 面板，使其仅显示四个分类状态行且不显示“已扫描完成”标题；验证各状态下列表布局紧凑

## 3. 规格与验证

- [ ] 3.1 根据实现结果同步 proposal、design、spec 和 tasks，执行 `openspec validate relocate-scan-progress-prompt --type change --strict` 验证变更文档有效
- [x] 3.2 执行 `git diff --check` 和 Spotless Debug 构建，确认构建成功后关闭旧实例并启动最新构建的 App
