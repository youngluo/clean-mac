## 1. 语言基础设施

- [x] 1.1 添加 `AppLanguage` 模型、支持语言列表、系统语言回退逻辑和 UserDefaults 持久化。
- [x] 1.2 添加 `Localizable.xcstrings`，为简体中文和 English 提供菜单、界面、扫描、结果和错误文案。

## 2. 右键菜单与状态接入

- [x] 2.1 在“主题”菜单后增加“语言”子菜单，显示三种语言选项和当前选中状态。
- [x] 2.2 将语言状态注入 SwiftUI 根视图，空闲状态切换后立即刷新菜单和主界面。
- [x] 2.3 在扫描、清理和结果状态下禁用语言选项，保持当前会话语言一致。

## 3. 界面与扫描文案本地化

- [x] 3.1 迁移 SwiftUI 视图中的固定文案到语义化本地化 key，并检查动态插值和长文本布局。
- [x] 3.2 迁移模型、ViewModel、扫描服务和清理结果中的应用生成文案到语义化本地化 key。
- [x] 3.3 保留文件名、路径、Bundle ID 和无法分类的外部命令错误原文，不翻译用户数据。
- [x] 3.4 保持主题切换、扫描策略、候选选择和清理执行行为不变。
- [x] 3.5 识别已知文件操作权限错误并按当前应用语言显示，无法分类的外部错误继续保留原文。

## 4. 验证

- [x] 4.1 验证三种语言模式的选择、持久化、系统语言回退和菜单选中状态。
- [x] 4.2 验证空闲状态可切换语言，扫描、清理和结果状态不可切换。
- [x] 4.3 验证中英文主界面、右键菜单、扫描进度、候选状态和错误文案无混排。
- [ ] 4.4 运行 XCTest、Debug 构建，关闭旧实例并启动最新构建验证。（测试宿主在当前 macOS 环境中启动后提前退出，Debug 构建与测试包构建已通过。）

## 5. 国际化架构收口

- [x] 5.1 将 `LanguageStore` 收口为唯一语言状态源，移除业务层和 `L10n` 对 UserDefaults 的隐式读取。
- [x] 5.2 引入可编码、可跨任务传递的 `LocalizedMessage`，将扫描进度、诊断、provider 状态、候选保护原因和清理结果改为消息值或原始数据。
- [x] 5.3 迁移 CleanerService、CleanerViewModel 和错误处理，禁止扫描热路径提前解析本地化字符串。
- [x] 5.4 统一数量、体积和失败信息格式化，保证显式使用当前 locale 并保留外部原文。
- [x] 5.5 增加消息双语渲染、Catalog key 完整性、结构化错误和无 `L10n.current` 静态检查。
