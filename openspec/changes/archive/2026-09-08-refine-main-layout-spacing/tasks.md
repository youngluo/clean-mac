## 1. 布局间距调整

- [x] 1.1 在共享主题文件中新增语义化布局间距，并迁移当前生效视图中的 padding 与 Stack spacing；通过差异检查确认没有修改清理逻辑和状态行为
- [x] 1.2 为工作态圆形主操作增加底部视觉缓冲；通过源码检查确认圆球顶部定位、尺寸和根布局位置保持不变
- [x] 1.3 调整 0 项 provider 的状态图标并保留有候选 partial 的警示图标；通过构建和状态映射检查验证
- [x] 1.4 将圆球呼吸效果限定为扫描状态，并通过拆分扫描/静态视图避免循环动画状态残留；通过源码检查确认清理执行和结果状态不会触发呼吸动画
- [x] 1.5 在统一候选入口过滤实际大小为 0 KB 的候选项并保留大小未知项；通过源码检查和 Debug 构建验证
- [x] 1.6 在候选列表标题同时显示已选大小与候选总大小，并使用 `/` 分隔；通过本地化检查和 Debug 构建验证
- [x] 1.7 仅将候选项名称设为可选区域并保持不可选状态禁用；通过源码检查和 Debug 构建验证
- [x] 1.8 清理执行期间保留并禁用“移到废纸篓”和“完成”按钮，清理完成后恢复完成操作；通过状态条件检查和 Debug 构建验证

## 2. 清理未使用视图

- [x] 2.1 确认 `CompletedView` 没有源码或项目引用并删除文件；通过全仓库搜索确认无残留引用

## 3. 验证

- [x] 3.1 执行 Debug 构建并确认成功
- [x] 3.2 关闭旧 CleanMac 实例并启动最新构建，检查空闲、扫描和确认状态的面板间距与 Git diff --check

## 4. 主题色与图标对齐

- [x] 4.1 将主操作、强调、扫描中、成功、警告和失败状态改为图标主导的语义色，保留系统材质和菜单栏模板图标行为；通过 Debug 构建与运行验证
- [x] 4.2 隐藏成功候选项的重复结果文案，保留成功图标并继续展示失败、跳过和取消信息；通过 Debug 构建验证
- [x] 4.3 将“移到废纸篓”固定为操作区最左侧按钮，保持重新扫描和完成/取消按钮的既有行为；通过 Debug 构建验证
- [x] 4.4 提亮主操作、成功和警告颜色，并提高主弹出层与内容面板的毛玻璃凝实度；通过 Debug 构建和运行验证
- [x] 4.5 在毛玻璃下方增加动态中性系统底色，移除面板整体品牌色倾向；通过 Debug 构建和运行验证
- [x] 4.6 统一 NSPopover 外壳与 SwiftUI 根视图背景，保留内部面板毛玻璃效果并消除背景分界；通过 Debug 构建和运行验证
- [x] 4.7 将选中、成功、警告和失败状态改为 macOS 动态系统色，保留品牌绿用于主操作和品牌强调；通过 Debug 构建和运行验证
- [x] 4.8 将正在扫描状态改为 macOS 动态 systemBlue，保持与成功状态的语义区分；通过 Debug 构建和运行验证
- [x] 4.9 将选中状态改为 macOS 动态 systemBlue，保持与成功状态的语义区分；通过 Debug 构建和运行验证
- [x] 4.10 清理完成后保持候选列表与滚动位置，不因状态切换重新挂载 `CleaningView`；通过源码检查、Debug 构建和运行验证
- [x] 4.11 将圆形主操作与“移到废纸篓”按钮统一为深鼠尾草绿与暖象牙白；通过 Debug 构建和运行验证
- [x] 4.12 同步 NSPopover、HostingView 与 SwiftUI 根背景的动态外观，消除箭头与内容区的颜色不一致；通过 Debug 构建和运行验证
- [x] 4.13 移除 SwiftUI 根视图的不透明背景，交由 NSPopover 原生材质统一箭头与内容外壳；通过 Debug 构建和运行验证
- [x] 4.14 将完全磁盘访问提示的图标与操作链接统一为 systemBlue，保留其他警告状态的系统橙；通过 Debug 构建和运行验证
- [x] 4.15 将完全磁盘访问提示图标替换为 `lock.shield`，避免 `lock.open` 产生错误的解锁语义；通过 Debug 构建和运行验证
- [x] 4.16 移除完全磁盘访问提示中“去设置”右侧的重新检查图标，保留文字设置入口；通过 Debug 构建和运行验证
- [x] 4.17 为完全磁盘访问提示使用轻量毛玻璃和半透明中性底色，降低卡片背景存在感并保持文字可读性；通过 Debug 构建和运行验证
- [x] 4.18 将扫描进度和可清理项目面板同步为与权限提示一致的轻量毛玻璃样式，并保留各自圆角与布局；通过 Debug 构建和运行验证
- [x] 4.19 将品牌色和主操作色统一替换为 `#3FA796`，保留系统语义状态色；通过 Debug 构建和运行验证
- [x] 4.20 统一主题色为 `#3FA796`，将辅助色 `#E8D9B5` 限定为低频细节，移除独立警告色并调整主操作前景对比度；通过 Debug 构建和运行验证
- [x] 4.21 使用稳定的动态背景色和统一面板色替换透明底层，保留轻微材质质感并避免桌面背景影响颜色；通过 Debug 构建和运行验证（当前视觉方案由 5.x 重新校准为透明毛玻璃）
- [x] 4.22 移除主界面顶部的 `CleanMac` 文字标识，保留启动磁盘信息和菜单栏入口；通过 Debug 构建和运行验证
- [x] 4.23 将圆形主操作内的文字改为白色，保留主操作按钮的深青黑文字；通过 Debug 构建和运行验证
- [x] 4.24 将圆形主操作内文字改为与扫描进度面板正文一致的动态主文本色；通过 Debug 构建和运行验证
- [x] 4.25 将浅色与深色主题的根背景、统一面板表面重新校准，减少近白面板的漂浮感并保留轻微材质质感；通过 Debug 构建和运行验证（当前视觉方案由 5.x 重新校准为透明毛玻璃）
- [x] 4.26 将“移到废纸篓”按钮文字与圆球文字统一为动态主文本色；通过 Debug 构建和运行验证
- [x] 4.27 将圆球和“移到废纸篓”按钮文字统一恢复为白色；通过 Debug 构建和运行验证
- [x] 4.28 移除 SwiftUI 根视图的不透明背景，恢复由 NSPopover 原生材质统一承接尖角与根内容区；通过 Debug 构建和运行验证

## 5. 透明毛玻璃背景重校准

- [x] 5.1 根据参考图确认目标为随桌面变化的透明毛玻璃，不复制参考图中的菜单内容或布局，并同步更新 proposal、design 和 spec
- [x] 5.2 从 `GlassPanelModifier` 和 `SubtleGlassPanelModifier` 中移除不透明的 `Color.theme.panel` 底层，保留 `regularMaterial`/`thinMaterial` 与低对比度描边；同时移除不再使用的自适应主题面板色定义
- [x] 5.3 保持 `NSHostingController.view.layer` 和面板内容窗口透明，根内容不重复承载 AppKit 材质，由单层面板背景视图使用 `.menu` 材质承担模糊职责
- [x] 5.4 复核三个内容面板的材质层级，在不改变现有布局和交互的前提下，让桌面背景只柔和透出且不出现固定浅蓝或浅灰绿色块；根内容使用自适应中性基底稳定偏白效果
- [ ] 5.5 在白色、蓝色和高对比度桌面背景，以及浅色、深色和跟随系统主题下检查空闲、扫描、等待确认和清理完成状态
- [ ] 5.6 执行 Debug 构建，关闭旧 CleanMac 实例并启动最新构建；通过 `git diff --check` 和实际运行确认面板边缘、根内容区连续且文字可读

## 6. 单层面板背景重构

- [x] 6.1 将主界面承载从 `NSPopover` 改为无边框 `NSPanel`，保留状态栏定位、清理中保持打开和普通状态点击外部关闭行为
- [x] 6.2 使用单个 `NSVisualEffectView(.menu)` 承接面板背景，叠加自适应 `windowBackgroundColor` 中性底色，避免灰厚并降低桌面背景影响
- [x] 6.3 保持 SwiftUI 根内容、HostingView 和面板窗口透明，并接回主题同步、动态高度和面板外观更新
- [ ] 6.4 在白色背景下与参考图对照检查，并补充蓝色/高对比度背景及浅色、深色、系统主题验证

## 7. 可清理项目列表高度

- [x] 7.1 将可清理项目卡片最大高度从 `720pt` 调整为 `640pt`，超出后仅保持列表内部滚动，不改变其他工作态区域布局
- [x] 7.2 在屏幕可用高度不足时动态降低可清理项目卡片上限，并将主面板高度限制在可视区域内

## 8. App Icon 与主题色同步

- [x] 8.1 将现有 App Icon 的背景色从 `#5A9B6B` 重新生成至当前主题色 `#3FA796`，保留米白六边形、几何比例和透明圆角
- [x] 8.2 重新生成 `icon_16.png`、`icon_32.png`、`icon_64.png`、`icon_128.png`、`icon_256.png`、`icon_512.png` 和 `icon_1024.png`，保持 `Contents.json` 映射不变；菜单栏 template 资源不改动
- [x] 8.3 校验所有 App Icon 尺寸、主色、透明四角和菜单栏资源未变化，并通过资源检查与 Debug 构建验证
- [x] 8.4 增加与当前 PNG 资源保持一致的 `icon-source.svg` 备用源文件，并校验主题色、六边形结构和透明圆角
