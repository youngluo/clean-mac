## 1. 建立一次遍历的目录统计

- [x] 1.1 定义目录聚合结果和文件候选观察项，保存实际大小、逻辑大小、文件身份和完整性
- [x] 1.2 改造缓存、项目产物和应用残留扫描，使用一次聚合结果创建候选
- [x] 1.3 删除目录候选在 `appendExisting` 中的二次逻辑大小遍历

## 2. 共享统一扫描结果

- [x] 2.1 在 `scanUnified` 创建本次扫描上下文并贯穿各 provider
- [x] 2.2 登记完整扫描目录，空间分析命中时复用统计并合并磁盘占用
- [x] 2.3 在首次目录遍历中保留适用的安装包、压缩包和普通大文件观察项
- [x] 2.4 对不完整、失败或取消的目录禁用复用，保留补扫和部分完成语义
- [x] 2.5 防止父目录和嵌套文件候选重复计入
- [x] 2.6 限制空间分析观察项只在可分析目录中收集，避免拖慢缓存扫描
- [x] 2.7 移除临时文件和项目产物扫描中的 `lsof` 占用检查，改用清理前复核
- [x] 2.8 删除占用检查超时诊断、外部进程分支和无调用辅助方法

## 3. 验证

- [x] 3.1 添加目录大小一次遍历和逻辑大小复核测试
- [x] 3.2 添加共享目录复用、未归属目录补扫和不完整目录测试
- [x] 3.3 执行 `git diff --check` 和 Debug 构建
- [x] 3.4 运行测试或记录 XCTest runner 环境阻塞原因

验证记录：`build-for-testing` 通过；`xcodebuild test` 在 XCTest runner 建立连接前以 code 0 退出，报 `Early unexpected exit`，同时存在 macOS 13 目标链接 macOS 14 XCTest 的系统警告。

## 4. 清理会话授权与权限状态

- [x] 4.1 在首次特权清理前建立当前清理会话的管理员授权上下文，并将同一个 `AuthorizationRef` 传入每次特权命令执行
- [x] 4.2 在开始新扫描、完成、取消或服务释放时释放授权上下文，不保存密码
- [x] 4.3 在应用重新激活和面板重新打开时刷新完全磁盘访问状态
- [x] 4.4 添加授权会话生命周期和权限刷新入口的验证，并重新执行构建
- [x] 4.5 当前用户临时文件使用系统废纸篓 API，避免不必要的管理员特权命令
- [x] 4.6 将临时文件候选的最短未修改时间调整为 15 天，并保留旧日志的 7 天规则

验证记录：特权命令改为使用会话内同一个 `AuthorizationRef` 执行；当前用户临时文件改用系统废纸篓 API；临时文件候选阈值为 15 天且旧日志仍保持 7 天；Debug 构建、`build-for-testing` 和 `git diff --check` 通过；完整 `xcodebuild test` 仍因 XCTest runner 在建立连接前以 code 0 退出而失败。
