## 1. 根背景材质

- [x] 1.1 将 `MenuMaterialView` 的根背景调整为单一毛玻璃，移除 `windowBackgroundColor` 灰色 tint。
- [x] 1.2 保留有效外观同步、圆角、细边框和阴影行为。
- [x] 1.3 根材质从 `.popover` 换回 `.menu`，与 App 自身右键菜单同族；随后按实测把提亮值标定到菜单表面 `R244 G246 B247`。

## 2. 纯白提亮层

- [x] 2.1 在 `MenuMaterialView` 中于根材质之上增加纯白低 alpha 提亮视图（当前 `0.45`，可调），与根材质一同参与 `layout()`。
- [x] 2.2 提亮层仅在亮色外观下生效，在 `viewDidChangeEffectiveAppearance` 流程中同步；暗色外观下 alpha 取 0。
- [x] 2.3 确认提亮层不影响命中测试：z-order 位于根材质之上、SwiftUI hosting 之下。
- [x] 2.4 亮色下将 `SubtleGlassPanelModifier` 的三个内容卡片统一为 `#F7F7F7`；暗色内容卡片使用独立的低 alpha 提亮。
- [x] 2.5 将 `取消` 按钮从 `.buttonStyle(.bordered)` 统一到 `ThemeSecondaryButtonStyle`，消除提亮后面板内最深的填充块。
- [x] 2.6 在 `MenuMaterialView` 中补内侧纯白亮边（`rimView`，宽度 `0.5pt`、圆角随主圆角内缩），亮色下 alpha `0.45`、暗色下为 0，仅用于轮廓感。
- [x] 2.7 将亮色外观下的外侧描边收细至 `0.05`，暗色维持 `0.35`，避免近白表面上的线条过重。
- [x] 2.8 边缘收细：亮色外侧描边收至 `0.05`，内侧亮边宽度收至 `0.5pt`、alpha `0.45`。
- [x] 2.9 根据最终设计不采用自绘投影校准，保留现有系统阴影策略，避免为调节阴影引入额外窗口几何改动。

## 3. 规格与验证

- [x] 3.1 执行 `git diff --check`，确认变更不影响现有未提交工作。
- [x] 3.2 执行 Spotless Debug 构建。
- [x] 3.3 关闭旧版 Spotless 并启动最新构建。
- [ ] 3.4 逐行取色复核：面板表面与内容卡片均不低于输入法菜单的同高度读数，且卡片不再出现比表面更暗的灰块。
- [ ] 3.5 在彩色或深色背景上复核：表面读数落在 `#F0`~`#F8` 且能读出背景冷暖色偏；暗色主题下无白色提亮。

## 4. 按钮状态一致性

- [x] 4.1 抽出 `ButtonSurface` 共用常量（圆角、字号、按下透明度 `0.75`、禁用透明度 `0.65`），两套 pill 样式共用。
- [x] 4.2 主按钮禁用填充从 `panelBorder` 改为与次按钮同源的 `panelTint`，消除"禁用按钮比可点次按钮更重"；`panelBorder` 恢复只作描边。
- [x] 4.3 禁用文字统一 `textSecondary`，两套样式的禁用重量一致。
- [x] 4.4 执行 Spotless Debug 构建，关闭旧实例并启动最新构建。
- [ ] 4.5 在待确认状态下取色复核：无选中项时"移到废纸篓"的填充不重于同一行可点的"取消"。

## 5. 暗色面板底色

- [x] 5.1 记录暗色系统玻璃在不同桌面背景下的透底差异；按用户提供的参考效果，保留桌面颜色经模糊后透出的表现。
- [x] 5.2 移除 `MenuBarView` 暗色根面板上的固定实色填充，恢复半透明材质效果。
- [x] 5.3 暗色根面板沿用 AppKit 系统材质，不叠加固定 SwiftUI 背景或白色提亮层。
- [x] 5.4 浅色路径、辅助色 `#E8D9B5`（仍用于部分完成状态）及其它颜色保持不变；暗色卡片微调见第 6 节。
- [x] 5.5 调整后执行 Spotless Debug 构建，关闭旧版并启动最新构建。
- [ ] 5.6 在暗色外观下截屏复核：面板呈深灰半透明效果，桌面颜色经模糊后仍可辨认；确认其它颜色保持不变。

## 6. 暗色内容卡片提亮

- [x] 6.1 为 `SubtleGlassPanelModifier` 的暗色内容卡片保留 `0.06` 低 alpha 白色提亮，移除重复的 `thinMaterial`，使用根面板的模糊透底。
- [x] 6.2 修改后执行 Spotless Debug 构建，关闭旧版并启动最新构建。
- [ ] 6.3 在暗色外观下截屏复核：内容卡片背景略浅、与根面板区分自然，其它颜色不变。

## 7. 跟随系统主题初始化

- [x] 7.1 清除“跟随系统”模式下 App、面板和 HostingView 的固定外观覆盖，改为继承 macOS 当前外观。
- [ ] 7.2 在系统亮色外观下冷启动并确认面板不再固定为暗色；切换系统外观后复核面板同步更新。
- [x] 7.3 执行 Spotless Debug 构建，关闭旧版并启动最新构建。

## 8. 亮色内容卡片背景

- [x] 8.1 将状态、可清理项目和权限提示三个内容卡片的亮色背景统一为 `#F7F7F7`，并通过 `0.5` 不透明度在保持毛玻璃透底的同时提升背景可读性；亮色边框使用 `Color.primary.opacity(0.2)`，保留圆角。
- [ ] 8.2 在亮色外观下截屏复核三个卡片的背景一致，暗色外观和其它颜色不变。
- [x] 8.3 执行 Spotless Debug 构建，关闭旧版并启动最新构建。

## 9. 亮色毛玻璃雾化

- [x] 9.1 为 macOS 26 的 Liquid Glass 增加低透明度中性白雾化层，按 `0.65` 比例降低彩色桌面过度透出；暗色保持关闭。
- [ ] 9.2 在蓝色或高饱和桌面背景上截屏复核：主面板仍有透底模糊，但不被背景明显染色。
- [x] 9.3 执行 Spotless Debug 构建，关闭旧版并启动最新构建。

## 10. 圆球下方提示文案

- [x] 10.1 移除扫描过程中显示在主操作圆球下方的 provider detail 文案句号，中英文保持无句号的短语形式。
- [x] 10.2 让扫描阶段事件更新圆球下方的当前阶段提示，避免高层阶段文案在 ViewModel 中被忽略。
- [x] 10.3 将扫描提示中的内部或绕口表达收敛为直接短句，包括“正在准备扫描”“正在扫描缓存”“正在扫描大文件”“正在扫描启动磁盘目录”和“扫描已部分完成”，并去掉缓存、项目和应用扫描中与 provider 详细提示重复的文案；英文同步调整。
- [x] 10.4 执行 Spotless Debug 构建，关闭旧版并启动最新构建。
- [x] 10.5 将项目 provider 详细提示中的 `node_modules` 改为面向用户的“项目产物”描述，中英文同步调整。

## 11. 亮色结果列表分隔线

- [x] 11.1 将“可清理项目”标题与列表之间的分隔线在亮色主题下调整为 `0.25` 不透明度，暗色主题保持 `0.45`。
- [x] 11.2 执行 Spotless Debug 构建，关闭旧版并启动最新构建。

## 12. 提示层级与辅助文案

- [x] 12.1 将圆球下方提示改为与权限卡片说明一致的系统次要文字颜色。
- [x] 12.2 移除主圆球按钮上的辅助 Tooltip，并删除对应未使用的本地化键。
- [x] 12.3 清理其它未被代码引用的旧本地化键，保持 `L10nKey` 与 `Localizable.xcstrings` 一一对应。
