## 1. OpenSpec 与扫描边界

- [x] 1.1 固化空间分析的标准目录优先、媒体保护和不限制候选数量的行为，并通过 `openspec validate --strict` 验证 change 文档
- [x] 1.2 明确启动卷身份和系统保护路径的测试夹具，验证不会进入外部卷、网络卷或系统运行目录

## 2. 空间分析实现

- [x] 2.1 调整启动磁盘遍历顺序，优先发现 Downloads、Desktop、Documents 中逻辑大小大于 10 MB 的普通文件，并保持已有大小字段和候选排序
- [x] 2.2 收窄媒体排除规则，只保护系统 Photos/Music 应用及明确关联数据，验证用户媒体目录中的普通文件仍可进入候选评估
- [x] 2.3 修正卷边界、系统保护目录、符号链接和元数据读取失败处理，验证异常只导致 partial/诊断而不静默漏掉扫描结果
- [x] 2.4 对启动卷空间统计和候选发现使用统一文件身份去重，验证硬链接不会重复计数或生成重复候选
- [x] 2.5 保持空间分析没有候选数量限制，验证现有候选大小显示无变更
- [x] 2.6 对空间分析的 partial 且有候选状态显示成功勾选，验证其它 provider 的 partial 图标规则不变
- [x] 2.7 让空间分析仅跳过缓存清理、项目清理和应用残留实际生成的候选路径及其子项，验证统一候选列表不重复展示前三类内容且保留无候选时的空间分析兜底

## 3. 验证与收尾

- [x] 3.1 补充或更新空间分析单元测试，覆盖 Downloads 大文件、普通 Pictures/Music/Movies 文件、Photos/Music 保护数据、超时优先级、硬链接、读取失败、前序候选归属去重和无前序候选时的兜底统计
- [x] 3.2 运行 `git diff --check`、OpenSpec 严格校验和 Spotless Debug 构建，确认实现与规格一致

## 4. 项目产物标记闸门

- [x] 4.1 `scanDeveloper` 在名称命中后增加项目特征标记判定：从产物目录父目录向上探测至扫描根，无标记则丢弃候选
- [x] 4.2 补充单元测试：通用名无标记目录（如 `~/Documents/素材/build`）不入选、monorepo 上层标记仍命中、30 天近期保护与既有产物候选行为不变
- [x] 4.3 运行 OpenSpec 严格校验与 Debug 构建，替换运行实例验证
- [x] 4.4 收窄通用 `.app` 后缀排除：bundle-ID 风格命名的应用数据目录不再整树排除，回归测试为 `testUnifiedScanOnlySuppressesPathsOwnedByEarlierProviders`
