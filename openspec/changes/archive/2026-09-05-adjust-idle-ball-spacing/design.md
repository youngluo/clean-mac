## Context

截图显示空闲状态的圆形主操作与上下文信息之间缺少足够的呼吸空间。现有布局分别由 `CleanupHomeView` 和 `IdleView` 的 `VStack` 间距控制。

## Decision

只调整空闲状态的两个垂直间距：外层控制磁盘信息与空闲内容的距离，内层控制圆球、说明文字和权限提示卡片的距离。圆球大小、动效、颜色以及清理中和结果状态保持不变，避免引入额外的布局测量或状态逻辑。

## Verification

- 检查修改只影响空闲状态的垂直间距。
- 执行项目要求的 Debug 构建。
- 关闭旧实例并启动最新 CleanMac。
