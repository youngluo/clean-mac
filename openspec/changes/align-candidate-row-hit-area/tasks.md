## 1. 候选行整行交互

- [x] 1.1 将可选候选项的选择操作收敛到单个整行按钮，移除图标和名称上的嵌套按钮，并保留不可选与清理结果行的非交互状态；通过源代码检查确认行内不再存在嵌套选择按钮，并保留右键排除菜单

## 2. 候选列表宽度与边距

- [x] 2.1 为列表内容、分组标题和候选行建立一致的全宽布局约束，复用现有水平间距常量，使整行命中边界与分组标题内容边界对齐；通过 Debug 构建确认 SwiftUI 布局代码可编译
- [x] 2.2 移除候选行按下态的额外颜色反馈，固定名称文字颜色并保留选择图标的状态变化；通过源代码检查确认未修改共享操作按钮样式，并通过 Debug 构建验证

## 3. 规格与集成验证

- [x] 3.1 校验 change 的 proposal、delta spec、design 和 tasks 完整且格式有效，并通过 `openspec validate align-candidate-row-hit-area --type change --strict` 与 `git diff --check`
- [x] 3.2 重新构建 Debug App，关闭旧版 CleanMac 并启动最新构建产物，确认应用可正常启动并可用于检查整行点击、边距对齐及右键菜单
