## 1. 使用确认稿建立资源基准

- [x] 1.1 以用户最后确认的 PNG 为视觉基准，生成匹配原图比例与细节的 `icon_1024.png`，并让圆角背景外的区域保持透明。
- [x] 1.2 从确认稿的白色叶片提取简化黑色模板图形，生成菜单栏 PNG，并移除旧的未分配 SVG 源文件。

## 2. 生成资源目录图片

- [x] 2.1 根据确认稿重新生成 `icon_16.png`、`icon_32.png`、`icon_64.png`、`icon_128.png`、`icon_256.png`、`icon_512.png` 和 `icon_1024.png`，保持现有 `Contents.json` 映射不变。
- [x] 2.2 根据确认稿的白色叶片提取模板图形，生成 18 × 18 的 `menubar-icon.png` 和 36 × 36 的 `menubar-icon@2x.png`。
- [x] 2.3 验证所有 App Icon 与菜单栏 PNG 都包含 alpha 通道，且四角透明，同时保留已确认的叶子颜色与形状。

## 3. 验证并启动应用

- [x] 3.1 校验资源目录 JSON，以及所有生成图片的尺寸、alpha 通道和透明角。
- [x] 3.2 按项目要求执行 CleanMac Debug 构建，关闭旧实例并启动最新构建的应用，进行视觉检查。
- [x] 3.3 检查 Git diff 和工作区状态，确认改动范围仅包含图标资源与本 change 文档，并保留无关的 `AGENTS.md` 修改。

## 4. 提高菜单栏图标可见性

- [x] 4.1 在不改变叶片轮廓的前提下，将菜单栏图标有效绘制区域放大到约 14 × 14 像素，并同步生成 1x/2x PNG。
- [x] 4.2 验证放大后的菜单栏图标仍保持透明画布、模板渲染配置和清晰边缘。

## 5. 加强菜单栏识别度

- [x] 5.1 将菜单栏图标改为更饱满的黑色叶片轮廓，保留叶尖、茎部和简短叶脉切口，使其在 18px 下清晰可辨。
- [x] 5.2 重新生成 1x/2x PNG，验证可见区域、透明边距和模板资源配置。
